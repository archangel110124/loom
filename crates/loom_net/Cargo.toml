[package]
name = "loom_net"
version = "0.0.0"
edition = "2024"
publish = false

# **No dependencies, and TCP rather than UDP.** See ADR 0091: lockstep already
# has to wait for a missing input, so ordered reliable delivery is not a cost it
# is paying for nothing — it is exactly the guarantee it needs. `std::net` has
# that, so this crate has no dependencies at all.
[dependencies]

[lints]
workspace = true
