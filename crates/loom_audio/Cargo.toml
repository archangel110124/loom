[package]
name = "loom_audio"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
cpal = "=0.18.1"
loom_physics = { version = "0.0.0", path = "../loom_physics" }
