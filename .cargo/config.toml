[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[alias]
# The second of the three green checks. See xtask/src/main.rs.
xtask = "run --quiet --package xtask --"
