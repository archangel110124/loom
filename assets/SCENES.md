# The scene library

Generated by `tools/scene_index.py`. Re-run it after adding or removing a scene.

**Gates** — what actually checks the scene, which is the only reason most of these exist:

- `image` — in `GOLDEN`: its pixels are compared against `tests/references/` every run.
  A scene belongs here when it covers a **rendering path** nothing else covers.
- `validate` — in `SCENES`: rendered by `cargo xtask validate`, which requires zero Vulkan
  validation messages. Catches API misuse, not appearance.
- `cpu` — in the `--play` frame-budget list: measured against 30 ms/frame.
- `—` — in no gate. Either a fixture a Rust test loads directly, or something nothing
  guards. The second kind is where rot accumulates.

129 scenes: 57 in the image gate, 75 in validate.

| scene | gates | what it is for |
| --- | --- | --- |
| `games/deeper_demo.loom` | validate | ============================================================================= DEEPER — the demo ====================================================== |
| `games/fishing.loom` | — | DEEPER — the first playable slice. |
| `games/proving_ground.loom` | image,validate,cpu | PROVING GROUND — a whole game, in one text file. |
| `prefabs/channel_buoy.loom` | — | A channel mark: a float, a band, a pole and a topmark. |
| `prefabs/deckhand.loom` | — | THE DECKHAND -- a wooden artist's mannequin in black oilskin and an ochre sou'wester. |
| `prefabs/fishing_rod.loom` | — | Fishing rod + 5000-size spinning reel. |
| `prefabs/fishing_rod_bent.loom` | — | Fishing rod + 5000-size spinning reel. |
| `prefabs/jib_vi.loom` | — | JIB VI as a prefab — a nineteen-metre charter boat any scene can float. |
| `prefabs/jib_vi_decks.loom` | — | The deck of the JIB VI, as the boxes a person stands on. |
| `test/alpha_cutout.loom` | image,validate | Material::alpha_cutoff, shown by rendering the same card twice. |
| `test/bad_intensity.loom` | — | M1 error-path fixture. |
| `test/bare_sea.loom` | validate | Water and nothing else. |
| `test/beach.loom` | image,validate | A beach. |
| `test/blockout.loom` | validate | A small blockout, to prove the engine renders a real scene rather than a hardcoded triangle. |
| `test/camera.loom` | validate | A scene whose camera is authored rather than derived. |
| `test/campfire.loom` | image,validate | Night, lit by a fire and by nothing else. |
| `test/cascade.loom` | image,validate | A canyon waterfall into a still pool — reference image 62. |
| `test/cave.loom` | image,validate | A destructible voxel hillside with a tunnel carved through it. |
| `test/croft.loom` | validate | croft.loom — a stone longhouse on a grass shelf, late afternoon. |
| `test/deckhand_mirror.loom` | validate | THE DECKHAND IN A MIRROR — the scene that shows the thing you cannot see in a first-person game: your own character, animating, from the front. |
| `test/deckhand_walk.loom` | validate | THE DECKHAND WALKS — the scene that proves an articulated puppet animates. |
| `test/dripping.loom` | image,validate | A row of pipes dripping onto a tiled floor — reference image 63. |
| `test/emberfall.loom` | image,validate | The GPU particle pool, which is a rendering path nothing else here covers. |
| `test/explosion.loom` | image,validate | An explosion, authored out of parts the engine already had. |
| `test/fight_idle.loom` | — | The fight bench, nobody at the rod. |
| `test/fight_lazy.loom` | — | The fight bench, lazy pilot — and this is Pillar 4 as a regression test. |
| `test/fight_masher.loom` | — | The fight bench, masher pilot: reeling flat out and hammering the button. |
| `test/fight_skilled.loom` | — | The fight bench: a fish, a rod, and nobody's boat. |
| `test/fight_sloppy.loom` | — | The fight bench, sloppy pilot — the scene that proves there is a gradient. |
| `test/fishing_rod.loom` | — | The 2.3 m fishing rod and its 5000-size spinning reel, in profile. |
| `test/forest.loom` | image,validate,cpu | A scattered forest: the rules, not the trees. |
| `test/glass.loom` | image,validate | Material::opacity — blended transparency, shown against what it replaces. |
| `test/gleamsprat.loom` | image,validate | The gleamsprat, in the engine that has to draw it. |
| `test/gleamsprat_coral_only.loom` | — | The coral part alone — the rig that found the inverted normals, kept as the regression for them. |
| `test/gleamsprat_cruise.loom` | image,validate | The gleamsprat going somewhere. |
| `test/grass_slope.loom` | image,validate | Grass reading the terrain it stands on. |
| `test/ground.loom` | image,validate | Textured voxel ground. |
| `test/homestead.loom` | image,validate | A homestead on an island, in the rain. |
| `test/interact_probe.loom` | — | The digital-channel bench. |
| `test/jib_vi_drift.loom` | — | Five people standing still on a boat, for an hour of ticks. |
| `test/jib_vi_float.loom` | — | A nineteen-metre charter boat, floating. |
| `test/jib_vi_painted.loom` | — | A nineteen-metre charter boat, floating. |
| `test/jib_vi_stations.loom` | — | Twelve people standing on twelve named pieces of deck. |
| `test/jib_vi_underway.loom` | validate | The nineteen-metre boat, under way — the first thing in this repository that drives a floating hull. |
| `test/jib_vi_walk.loom` | — | Walking the whole boat. |
| `test/lanternhead.loom` | image,validate,cpu | lanternhead.loom — a fishing quay under a headland, low sun, in the rain. |
| `test/materials.loom` | image,validate | A scene that shows what a material actually does, by varying one thing at a time against a fixed reference. |
| `test/meadow.loom` | image,validate | Grass, as generated geometry. |
| `test/mirrorpool.loom` | image,validate | A still pool with something standing beside it. |
| `test/modern_house.loom` | — | A modern house, from a supplied first-floor plan. |
| `test/mood_deep.loom` | image,validate | The mood ladder, in pixels — ADR 0069. |
| `test/moraine.loom` | validate | A glacial vale, 256 x 64 x 256 m — the landscape §5 of the voxel-shape research describes, built out of everything the four commits before it added. |
| `test/mountain_pass.loom` | validate | A waymarker on a grass shoulder below the pass, late in the afternoon. |
| `test/ocean.loom` | image,validate | The sea, as a displaced surface. |
| `test/ocean_cinematic_stress_12m.loom` | — | STRESS TEST — not a shipping scene. |
| `test/ocean_cinematic_stress_14m.loom` | — | STRESS TEST — not a shipping scene. |
| `test/ocean_cinematic_stress_16m.loom` | — | STRESS TEST — not a shipping scene. |
| `test/ocean_cinematic_stress_24m.loom` | — | STRESS TEST — not a shipping scene. |
| `test/ocean_cinematic_stress_2m.loom` | — | STRESS TEST — not a shipping scene. |
| `test/ocean_cinematic_stress_32m.loom` | — | STRESS TEST — not a shipping scene. |
| `test/ocean_cinematic_stress_4m.loom` | — | STRESS TEST — not a shipping scene. |
| `test/ocean_cinematic_stress_8m.loom` | — | STRESS TEST — not a shipping scene. |
| `test/ocean_cinematic_stress_probe_fill.loom` | — | STRESS TEST — not a shipping scene. |
| `test/ocean_cinematic_stress_probe_thin.loom` | — | A driven hull ploughing a volumetric pool — reference image 64, in full. |
| `test/ocean_cinematic_stress_tiled.loom` | — | STRESS TEST — the obvious author workaround for "the ocean is bigger than 32 m": tile it out of two hero volumes. |
| `test/ocean_cinematic_stress_whole.loom` | — | STRESS TEST — the literal request: the whole ocean, cinematic. |
| `test/office.loom` | validate | M1 round-trip fixture. |
| `test/pbr_library.loom` | validate | The imported PBR library, one sphere each, on a floor of the first. |
| `test/plough.loom` | image,validate | A body ploughing a pool, and the foam it leaves behind it. |
| `test/plough_cinematic.loom` | image,validate | A driven hull ploughing a volumetric pool — reference image 64, in full. |
| `test/plough_cinematic_low.loom` | image,validate | The cinematic tank from the water's own level — extends, not a copy. |
| `test/plume.loom` | image,validate | A pyre and its plume, under a low sun. |
| `test/plume_close.loom` | image,validate | The same pyre as plume.loom, from inside the marched hull's own reach. |
| `test/plume_gale.loom` | image,validate | The same pyre in a gale, seen from across the wind. |
| `test/plume_roof.loom` | image,validate | A plume passing a gantry deck — the scene that can only pass with SV_Depth. |
| `test/pool.loom` | image,validate | A sphere dropped into a still pool. |
| `test/pool_jet.loom` | image,validate | The Worthington jet, in a scene where there is something to see. |
| `test/prefab_night.loom` | — | The same room after dark — extends rather than a copy. |
| `test/prefab_room.loom` | — | Three lamps from one prefab, each placed differently and one of them dimmed. |
| `test/prefabs/lamp.loom` | — | A prefab is an ordinary scene. |
| `test/primitives.loom` | image,validate | Every procedural primitive, to prove the library renders. |
| `test/props.loom` | validate | Every model imported from a download, at its real size, on ground. |
| `test/proving_ground.loom` | — | A scene that exercises the three things that are hard to get right, and that fail in ways a single screenshot hides: ray-traced shadows, collision aga |
| `test/puddles.loom` | image,validate | Puddles: standing water where the ground holds it. |
| `test/rain_gantry.loom` | image,validate | Rain under a MESH roof — ADR 0014's trigger 2, and the whole reason the drop simulation exists. |
| `test/rain_impact.loom` | image | Rain landing on wet ground — Phase 4 step 4, close enough to see it. |
| `test/rain_overhang.loom` | image,validate | Rain, and the roof that stops it. |
| `test/rain_pool.loom` | image,validate | Rain falling on a bounded pool, seen from close to the water. |
| `test/range.loom` | validate | A firing range: the trigger, end to end. |
| `test/ribbon.loom` | image,validate | A spout pouring a volumetric ribbon into a bounded cinematic pool. |
| `test/rig_adrift.loom` | — | THE CONTROL FOR THE ROUND TRIP: a catch you cannot land because she is not alongside — and the swim home that says the demo has no unrecoverable state. |
| `test/rig_ashore.loom` | — | Getting off her, on foot, with nothing held but a direction. |
| `test/rig_bump.loom` | — | Coming home, and holding W into the rig anyway. |
| `test/rig_deck.loom` | image,validate | The rig's deck field, and the proof that replacing a primitive with a mesh moves no physics. |
| `test/rig_drive.loom` | — | Driving the JIB VI. |
| `test/rig_fish.loom` | — | A fish, played from the cockpit of the JIB VI. |
| `test/rig_overboard.loom` | — | Overboard, and back. |
| `test/rig_reboard.loom` | — | Overboard from the BOAT, a hundred metres out, and back onto her. |
| `test/rig_salon.loom` | — | Can you get into the salon? The demo hub with the player already aboard, standing in the cockpit on the centreline of the deckhouse's after door. |
| `test/rig_shed.loom` | image,validate | The shed end, on its own camera. |
| `test/rig_structure.loom` | image,validate | The rig, assembled: deck, six piles, the tide seam, and all fourteen bulwark rail/cap runs, in one scene with one gate row watching it. |
| `test/rig_trip.loom` | — | THE ROUND TRIP — the demo's four features as one thing you do. |
| `test/rig_underhull.loom` | — | Can you swim through the boat? The demo hub with the player already in the sea, off her port quarter, one metre under the surface. |
| `test/rig_walk.loom` | — | Walking the Rig. |
| `test/river.loom` | image,validate | A river, and a crate that ends up downstream of where it was dropped. |
| `test/shore.loom` | image,validate | Where the water meets the land. |
| `test/slosh.loom` | image,validate | A bounded tank of volumetric water with a pillar in it, and a ball dropped in. |
| `test/smoke.loom` | image,validate | A smoke test, in both senses. |
| `test/spindrift.loom` | image,validate | A sea steep enough to throw spray — W5's first source, and the scene that covers it in both gates. |
| `test/splash.loom` | image,validate | Something falls in the sea. |
| `test/spout.loom` | image,validate | A stone spout pouring onto a tiled floor — reference image 60. |
| `test/spruce.loom` | image,validate | The first asset generated by Hunyuan3D and imported into Loom. |
| `test/squall.loom` | image,validate | A squall: raining here, dry there, under one sky. |
| `test/stoneyard.loom` | image,validate | A stone yard at low sun — the scene the secondary rays are for. |
| `test/terrain_billion.loom` | — | A billion voxels. |
| `test/terrain_stress.loom` | validate | A voxel world large enough to be a performance question rather than a demo. |
| `test/tower.loom` | validate | A physics demo: a stack of crates, a ramp, and a ball that knocks it over. |
| `test/tree_layered.loom` | validate | The same tree built in layers, beside the one-shot generation it replaces. |
| `test/trees.loom` | — | Eight imported trees, each wearing its own texture. |
| `test/turret_range.loom` | validate | A firing range: the trigger, end to end. |
| `test/underwater.loom` | image,validate | Under the surface, looking up. |
| `test/vale.loom` | image,validate | An eroded landscape, in a voxel volume, in a game. |
| `test/wake.loom` | image,validate | Two-way coupling, and the assertion that proves it — W6, ADR 0046. |
| `test/walker.loom` | validate | A character that walks, driven entirely by a script. |
| `test/water_crate.loom` | image,validate | A crate floating on the sea — the scene W5's exit criterion is measured on. |
| `test/weather_ramp.loom` | — | The carry instrument, on a sea that gets up while it runs. |
| `test/whitecaps.loom` | image,validate | Whitecaps, and the trail they leave behind them (W2). |
| `test/windy.loom` | image,validate | Wind, made visible. |
| `test/workshop.loom` | — | M5's exit criterion: an imported glTF mesh and procedural primitives together. |
